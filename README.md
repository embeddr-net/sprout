# Embeddr Sprout

Embeddr Sprout is a simplified example Embeddr client.
It uses @embeddr/zen-shell package - connects to an instance and shows its panel.

It's intended to be used as a reference for zen-ui package

If you want a headless example check out embeddr-leaf. It uses @embeddr/client-typescript to connect headlessly